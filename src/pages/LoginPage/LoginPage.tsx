import { LoginForm } from "@/components";
import { Module } from "@/components/ui";

export const LoginPage = () => {
    return (
        <Module>
            <LoginForm />
        </Module>
    );
};